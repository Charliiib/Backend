spring:
  datasource:
    url: jdbc:mysql://crossover.proxy.rlwy.net:33378/comparar?useSSL=false&serverTimezone=UTC
    username: root
    password: nGMRuCFJVUMAYvYjkueKGDVNtGQxPXQA
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  main:
    lazy-initialization: true
  mvc:
    async:
      request-timeout: 30000

server:
  port: ${PORT:8080}

logging:
  level:
    com.webapp.comparar: DEBUG